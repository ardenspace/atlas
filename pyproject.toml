[project]
name = "atlas"
version = "0.1.0"
description = "Market research + worldbuilding companion for marketing projects"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "httpx>=0.27",
]
